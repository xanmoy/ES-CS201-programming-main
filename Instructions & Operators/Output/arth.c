#include<stdio.h>
#include<math.h>
int main(){
    int a = 1 , b = 2, c , y, z; 
    int sum = a + b;
    int multiply = a * b;
    z = c + y;
    int power = pow(b,a);
    printf("%d\n", power);
    printf("%d", -16%10);

    return 0;
}